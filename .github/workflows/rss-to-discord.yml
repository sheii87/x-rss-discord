name: RSS to Discord

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "test = renvoie le dernier post ; cron = comme le schedule (nouveaux seulement)"
        type: choice
        options:
          - test
          - cron
        default: test

jobs:
  relay:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Relais RSS
        env:
          RSS_URL: ${{ secrets.RSS_URL }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          FORCE_TEST: ${{ github.event.inputs.mode != 'cron' && '1' || '0' }}
        run: |
          echo "event=${{ github.event_name }} mode=${{ github.event.inputs.mode }} FORCE_TEST=$FORCE_TEST"
          python3 << 'PY'
          import json, os
          from pathlib import Path
          cfg = {
            "rss_url": os.environ["RSS_URL"],
            "webhook_url": os.environ["DISCORD_WEBHOOK_URL"],
            "discord_username": "@kbourcrypto",
            "avatar_url": "https://pbs.twimg.com/profile_images/1877015930200260608/ymN_glL7_400x400.png",
            "interval_seconds": 600,
            "max_per_run": 5,
            "send_on_first_run": False,
          }
          Path("config.json").write_text(json.dumps(cfg), encoding="utf-8")
          PY
          python3 rss_to_discord.py --once

      - name: Sauver seen.json
        if: always()
        continue-on-error: true
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add seen.json
          if git diff --cached --quiet; then
            echo "Pas de changement seen.json"
            exit 0
          fi
          git commit -m "update seen.json"
          git pull --rebase origin main || true
          git push
